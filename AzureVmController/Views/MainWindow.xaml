<Window x:Class="Com.revo.AzureVmController.Views.MainWindow"
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
		xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
		xmlns:Converters="clr-namespace:Com.revo.AzureVmController.Converters"
		mc:Ignorable="d"
		Title="Azure VM controller" 
		Height="Auto" MinHeight="200" MaxHeight="400" 
		Width="Auto" MinWidth="300" MaxWidth="500"
		Icon="../cloud.ico" 
		ShowInTaskbar="False" 
		Topmost="True" 
		WindowStyle="None" 
		ResizeMode="NoResize" 
		SizeToContent="WidthAndHeight"
		WindowStartupLocation="Manual"
		Activated="MainWindow_OnActivated">
	<Window.Resources>
		<CollectionViewSource x:Key="VmItemsSource" Source="{Binding VmItems}" d:Source="{d:DesignData ../DesignerSampleData/VmListItemSamples.xaml}" />
		<Converters:BoolInverter x:Key="BoolInverter"/>
	</Window.Resources>
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="*" />
			<RowDefinition Height="Auto"></RowDefinition>
		</Grid.RowDefinitions>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="100" />
			<ColumnDefinition Width="Auto"/>
		</Grid.ColumnDefinitions>
		<ListBox ItemsSource="{Binding Source={StaticResource VmItemsSource}}" 
		         Height="Auto"
				 Grid.Row="0" Grid.ColumnSpan="2"
				 IsEnabled="{Binding Path=IsUpdating, Converter={StaticResource BoolInverter}}">
			<ListBox.ItemTemplate>
				<DataTemplate>
					<DockPanel Margin="2">
						<Rectangle Fill="Gray" Width="32" Height="32" DockPanel.Dock="Left"/>
						<TextBlock Text="{Binding Name}" VerticalAlignment="Center"/>
						<TextBlock Text="{Binding State}" VerticalAlignment="Center"/>
					</DockPanel>
				</DataTemplate>
			</ListBox.ItemTemplate>
		</ListBox>
		<ProgressBar Grid.Row="1" Grid.Column="0"
					 Orientation="Horizontal"
					 HorizontalAlignment="Left"
					 Width="100"
					 IsIndeterminate="True">
			<ProgressBar.Style>
				<Style TargetType="ProgressBar">
					<Setter Property="Visibility" Value="Hidden" />
					<Style.Triggers>
						<DataTrigger Binding="{Binding IsUpdating}" Value="True">
							<Setter Property="Visibility" Value="Visible" />
						</DataTrigger>
					</Style.Triggers>
				</Style>
			</ProgressBar.Style>
		</ProgressBar>
		<StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
			<Button HorizontalAlignment="Right" VerticalAlignment="Bottom" Click="Refresh_Clicked" Margin="2,2,2,2">Refresh</Button>
			<Button HorizontalAlignment="Right" VerticalAlignment="Bottom" Click="Settings_Clicked" Margin="2,2,2,2" IsEnabled="{Binding CanEditSettings}">Settings...</Button>
			<Button HorizontalAlignment="Right" VerticalAlignment="Bottom" Click="Exit_Clicked" Margin="2,2,2,2">Exit</Button>
		</StackPanel>
	</Grid>
</Window>































































































































































































































































































































































































































































































































